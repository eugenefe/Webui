<?xml version="1.0" encoding="UTF-8"?>
<page xmlns="http://jboss.org/schema/seam/pages"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://jboss.org/schema/seam/pages http://jboss.org/schema/seam/pages-2.3.xsd">

   <param name="firstResult" value="#{obligorList.firstResult}"/>
   <param name="sort" value="#{obligorList.orderColumn}"/>
   <param name="dir" value="#{obligorList.orderDirection}"/>
   <param name="logic" value="#{obligorList.restrictionLogicOperator}"/>

   <param name="from"/>
   <param name="id" value="#{obligorList.obligor.id}"/>
   <param name="name" value="#{obligorList.obligor.name}"/>
   <param name="rateCd" value="#{obligorList.obligor.rateCd}"/>
   <param name="indCd" value="#{obligorList.obligor.indCd}"/>

</page>
